def solution(arr, n):
    if len(arr)%2!=0: #홀수라면
        # arr[::2] +=n
        for i in range(0,len(arr),2):
            arr[i]+=n
    else: #짝수라면
        # arr[1::2] +=n
        for i in range(1,len(arr),2):
            arr[i]+=n
    return arr