def solution(arr, idx):
    for i in range(len(arr)):
        if arr[i]==1 and i>=idx:
            return i
    return -1    

# answer=0
# try:
#     answer = arr.index(1,idx)#arr에서 숫자 1의 인덱스를 찾고 검색 시작은 idx부터 한다는 의미 
# except:
#     answer= -1

# return answer   