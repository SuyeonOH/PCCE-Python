def solution(arr, queries):
    for i,j in queries:
        for k in range(i,j+1):
            arr[k]+=1
    return arr
# for (s,e) in queries:
# arr=[a+1 if s <= i <=e else a for i, a in enumerate(arr)]