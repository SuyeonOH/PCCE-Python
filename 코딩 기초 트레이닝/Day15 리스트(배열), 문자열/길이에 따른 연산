def solution(num_list):
    mul=1
    if len(num_list)>=11:
        return sum(num_list)
    else :
        for num in num_list :
            mul*=num
        return  mul
# from math import prod #곱하기 함수 사용
# return sum(num_list) if len(num_list)>=11 else prod(num_list)
    
# return eval ('+'.join(list(map(str,num_list))))     map(적용시킬 함수, 적용할 값들)
# return eval ('*'.join(list(map(str,num_list))))