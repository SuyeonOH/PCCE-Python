def solution(n, control):
    for c in control :
        if c =="w":
            n+=1
        elif c =="s" :
            n-=1
        elif c== "d" :
            n+=10
        else :
            n-=10
    return n

# key = dict(zip(['w','s', 'd', 'a'], [1,-1,10,-10])) key끼리 value끼리 저장할 수 o
# return n+sum([key[c] for c in control])

    #key = dict(zip([1,-1,10,-10],['w','s', 'd', 'a'] ))
    #for i in range(len(numLog)-1):
    #   answer+=key[numLog[i+1]-numLog[i]]
    