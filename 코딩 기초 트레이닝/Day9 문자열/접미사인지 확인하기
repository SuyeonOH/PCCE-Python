def solution(my_string, is_suffix):
    #suffix로 끝나는지를 확인해야 함
    if my_string.count(is_suffix,len(my_string)-len(is_suffix),len(my_string)):
        return 1
    else :
        return 0
#if m[-len(s):]== s :return 1
#return 0
    
#return int(my_string.endswith)